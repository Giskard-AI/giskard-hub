:og:title: Giskard Hub SDK - Evaluation Comparison
:og:description: Compare evaluation results to detect regressions and performance changes. Analyze metrics across different model versions and deployment stages using the Python SDK.

Compare evaluations
===================

After running evaluations, you can compare them to see if there are any regressions. We do not offer a built-in comparison tool in the SDK, but you can :ref:`use the Hub UI to compare evaluations <hub/ui/evaluations-compare>`.

